// UI styling is handled by Tamagui. Keep project-specific semantic values here only
// when they are shared outside Tamagui components, for example navigation metadata
// or non-visual domain presentation rules.
export const zonaCeroBrand = {
  emergencyOrange: '#D9480F',
  darkField: '#07111F',
  lightField: '#F6F8FB',
} as const;
