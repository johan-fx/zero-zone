# UI package placeholder

Owner: Equipo C or shared, depending on future reuse.

Reserved for shared UI only if web and mobile converge enough to justify the coupling. Keep mobile-specific Tamagui/Expo concerns in `apps/mobile` until extraction is proven useful.
