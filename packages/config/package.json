{
  "name": "@zona-cero/config",
  "private": true,
  "type": "module",
  "exports": {
    "./tsconfig/base": "./tsconfig.base.json",
    "./vitest/node": "./vitest.node.config.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:strict": "pnpm typecheck && pnpm test",
    "build": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vitest": "^4.1.0"
  }
}
